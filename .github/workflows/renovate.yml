name: Renovate

on:
  schedule:
    # Runs before 6am UTC on Monday (matching renovate.json schedule)
    - cron: '0 5 * * *'
  workflow_dispatch:  # Allows manual triggering

jobs:
  renovate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Run Renovate
        uses: renovatebot/github-action@6f08b38e8a246c2369efc4f626c30c0fd0b63efa # v40.3.12
        with:
          configurationFile: renovate.json
          token: ${{ secrets.GITHUB_TOKEN }}
        env:
          LOG_LEVEL: debug  # Set to 'info' or remove after initial testing
